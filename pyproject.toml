[project]
name = "croma-tools"
version = "0.1.0"
requires-python = ">=3.11"
# croma's only in-repo Python is tools/land.py (the push/PR/merge flow), which
# needs `requests`. The corpus-scale Python proving suite lives in croma-test.
dependencies = [
    "requests>=2.31.0",
]
